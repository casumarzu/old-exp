<div id="items">
    <% for(var i = 0; i < recipes.list.length; i++) {%>
        <div class="item">
        <h1><%= recipes.list[i].name %></h1>
        <img src="<%= recipes.list[i].pic %>" />
        </div>
    <% } %>
</div>